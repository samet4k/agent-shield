# Cursor Integration

1. Install AgentShield CLI
2. Run `agentshield init`
3. Install the VS Code extension from `extensions/vscode`
4. Set `agentshield.enabled: true` in settings

Cursor uses the integrated terminal; AgentShield proxies all shell commands automatically.